schema_version: 1

context:
  version: "0.9.1"
  name: "sparsax"
  python_min: "3.12"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/sparsax-${{ version }}.tar.gz
  sha256: 9c64b321b692c34d338f7f205e62893f028416feb038a79b1ff9a18f426be381

build:
  number: 1
  skip:
    - win
    - python < "3.12"
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python
    - scikit-build-core >=0.10
    - nanobind >=2.0
    - jax >=0.9.0
    - pip
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - cmake
    - ninja
    - suitesparse
    - make

  run:
    - python
    - jax >=0.9.0
    - numpy
    - suitesparse

tests:
  - python:
      imports:
        - sparsax
      pip_check: true

about:
  summary: JAX-native sparse direct solvers via SuiteSparse (CHOLMOD + KLU)
  license: BSD-3-Clause
  license_file: LICENSE.txt
  homepage: https://github.com/knaaptime/sparsax

extra:
  recipe-maintainers:
    - knaaptime
